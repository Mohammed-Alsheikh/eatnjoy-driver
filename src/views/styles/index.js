import {Dimensions} from 'react-native';

const Colors = {
  background: '#F2E8DA',
  primary: '#690D12',
  secondary: '#0A475C',
};

const {width, height} = Dimensions.get('window');

export {width, height};
export default Colors;
